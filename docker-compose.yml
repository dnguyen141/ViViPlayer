version: '3'

services:
  redis:
    image: redis:5
    expose:
      - "6379"
  django:
    build: ./viviplayer
    volumes:
      - ./viviplayer:/app/backend
    ports:
      - "8000:8000"
    links:
      - redis
  frontend:
    build: ./viviplayer-frontend
    volumes:
      - ./viviplayer-frontend:/app/frontend
    ports:
      - "3000:3000"

